\subsection{\href{https://beta.lmfdb.org/knowledge/show/group.permutation_representation}{Permutation representation}}
\begin{definition}\label{group.permutation_representation}
\uses{}
A \textbf{permutation representation} of a finite group $G$ is a homomorphism $\rho : G \to S_n$ for some $n$.  The integer $n$ is the \textbf{degree} of $\rho$, and a representation is \textbf{faithful} if $\rho$ is injective.  The representation is \textbf{transitive} if the set $\{1, \dots, n\}$ cannot be broken up into smaller orbits under the action of $G$, i.e. for all $1 \le i \le j \le n$ there is some $g \in G$ with $\rho(g)(i) = j$.
\end{definition}


