\subsection{\href{https://beta.lmfdb.org/knowledge/show/lattice.minimal_vector}{Minimal vector length }}
\begin{definition}\label{lattice.minimal_vector}
\uses{lattice.definition}
For an \hyperref[lattice.definition]{integral lattice}, $L$, the \textbf{minimal vector length}, denoted $m(L)$, is the length of the shortest non-zero vector in $L$.  A \textbf{minimal vector} in $L$ is a vector $\vec{v}$ such that $Q(\vec{v})=m(L)$.  
\end{definition}


