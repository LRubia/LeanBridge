\subsection{\href{https://beta.lmfdb.org/knowledge/show/cmf.embedding_format}{Embedding Format}}
\begin{definition}\label{cmf.embedding_format}
\uses{cmf.character,cmf.embedding_label,cmf.hecke_operator,cmf.level,cmf.newform,cmf.satake_angles,cmf.satake_parameters,cmf.weight,nf.embedding}
\leanok
Let $f$ be \hyperref[cmf.newform]{newform} of \hyperref[cmf.level]{level} $N$, \hyperref[cmf.weight]{weight} $k$ and \hyperref[cmf.character]{character} $\chi$.


Further, let $a_n$ denote the trace  of the \hyperref[cmf.hecke_operator]{Hecke operator} $T_n$.


For each \hyperref[nf.embedding]{embedding} $\iota$ of the coefficient field, we may consider the embedded modular form $\iota(f)$.
We present these embeddings sorted numerically according to the two values that define their \hyperref[cmf.embedding_label]{label}.


We may present $\iota(f)$ in four formats:

\begin{itemize}
\item  Complex embeddings: For each embedding $\iota$, we present the real and imaginary values $\iota(a_n)$ with the desired number of significant digits of the stored values.
\end{itemize}
The stored values are the approximations to 53 bits of $\iota(a_n)$ initially computed up to 200 bits of precision. 

\begin{itemize}
\item  Normalized embeddings: the same as above, but now we present $a_n$ analytically normalized, i.e., $\iota(a_n) / n^{(k-1)/2}$
\end{itemize}

\begin{itemize}
\item  \hyperref[cmf.satake_parameters]{Satake Parameters}: we use $\iota(a_p)$, these are deduced from the \hyperref[cmf.satake_angles]{Satake angles}.
\end{itemize}

\begin{itemize}
\item  \hyperref[cmf.satake_angles]{Satake Angles}: $\theta_p$ where $\theta_p \in [-\pi, \pi]$, the arguments of the \hyperref[cmf.satake_parameters]{Satake parameters}.
\end{itemize}

\end{definition}


