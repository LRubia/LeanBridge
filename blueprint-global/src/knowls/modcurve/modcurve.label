\subsection{\href{https://beta.lmfdb.org/knowledge/show/modcurve.label}{Label of a modular curve}}
\begin{definition}\label{modcurve.label}
\uses{gl2.genus,gl2.index,gl2.label,gl2.level,gl2.open,modcurve,modcurve.gassmann_class}
The \textbf{label} of a \hyperref[modcurve]{modular curve} $X_H$ is the same as the \hyperref[gl2.label]{label} of the \hyperref[gl2.open]{open subgroup} $H\le \GL_2(\widehat{\Z})$. When $\det(H)=\widehat{\Z}^\times$ and $-I\in H$ this label has the form $\texttt{N.i.g.c.n}$, where

\begin{itemize}
\item  $N$ is the \hyperref[gl2.level]{level} of $H$
\item  $i$ is the \hyperref[gl2.index]{index} of $H$
\item  $g$ is the \hyperref[gl2.genus]{genus} of $H$
\item  $c$ is a base-26 ordinal that uniquely identifies the \hyperref[modcurve.gassmann_class]{Gassmann class} of $H$ among groups of the same level, index, and genus.
\item  $n$ is a positive integer that distinguishes nonconjugate subgroups of the same level, index, genus, and Gassmann class.
\end{itemize}

When $\det(H)=\widehat{\Z}^\times$ and $-I\not \in H$ this label has the form $\texttt{N.i.g-M.c.m.n}$, where $N$, $i$, and $g$ are as above, and

\begin{itemize}
\item  $N$ is the level of $H$
\item  $i$ is the index of $H$
\item  $g$ is the genus of $H$
\item  $M$ is the level of $H':=\langle H, -I\rangle$
\item  $c$ is the base-26 ordinal that identifies the Gassmann class of $H'$
\item  $m$ is the positive integer in the label $\texttt{M.j.g.c.m}$ of $H'$, where $i=2j$.
\item  $n$ is a positive integer that distinguishes $H$ from nonconjugate refinements of $H'$ of the same level.
\end{itemize}
\end{definition}


