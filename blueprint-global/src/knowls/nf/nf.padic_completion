\subsection{\href{https://beta.lmfdb.org/knowledge/show/nf.padic_completion}{$p$-adic completion of a number field}}
\begin{definition}\label{nf.padic_completion}
\uses{lf.ring_of_integers,nf,nf.local_algebra,nf.prime,ring.dedekind_domain,ring.field_of_fractions,ring.fractional_ideal,ring.integral_domain,ring.irreducible}
Let $K$ be a \hyperref[nf]{number field}, $\mathcal{O}_K$ its \hyperref[lf.ring_of_integers]{ring of integers}, $\mathfrak{P}$ a non-zero \hyperref[nf.prime]{prime ideal} of $\mathcal{O}_K$, and $p\in \Z\cap \mathfrak{P}$.
There are a couple of ways to construct $K_{\mathfrak{P}}$, the \textbf{$p$-adic completion of $K$ at $\mathfrak{P}$}.

First, we can take the inverse limit
$$ \lim_{\leftarrow} \mathcal{O}_K/\mathfrak{P}^n $$
which is an \hyperref[ring.integral_domain]{integral domain}.  Its \hyperref[ring.field_of_fractions]{field of fractions} is $K_{\mathfrak{P}}$.

Second, since $\mathcal{O}_K$ is a \hyperref[ring.dedekind_domain]{Dedekind domain}, if $\alpha\in K^*$ the \hyperref[ring.fractional_ideal]{fractional ideal} 
$$\langle \alpha\rangle = \prod_{\mathfrak{Q}} \mathfrak{Q}^{e_{\mathfrak{Q}}}$$
where the product is over all non-zero prime ideals $\mathfrak{Q}$, all $e_{\mathfrak{Q}}\in\Z$, and all but finitely many $e_{\mathfrak{Q}}=0$.  Then we define
$v_{\mathfrak{P}}(\alpha)=e_{\mathfrak{P}}$, and then the metric $d$ on $K$ by
$d(\alpha, \beta) = p^{-v_{\mathfrak{P}}(\alpha-\beta)}$ if $\alpha\neq \beta$ and $d(\alpha,\alpha)=0$.  Then the completion of $K$ with respect to this metric is $K_{\mathfrak{P}}$.

If $K=\Q(a)$, and $f\in\Q[x]$ is the monic \hyperref[ring.irreducible]{irreducible} polynomial for $a$ over $\Q$, then adjoining the roots of $f$ to $\Q_p$ provide another means of constructing the completions.

Finally, the \hyperref[nf.local_algebra]{local algebra} of $K$, $\prod_{j=1}^g K_j$ is a product of the $p$-adic completions of $K$.  The $p$-adic completions of $K$ correspond to the nonarchimedian \hyperref[nf.place]{places} of $K$.
\end{definition}


