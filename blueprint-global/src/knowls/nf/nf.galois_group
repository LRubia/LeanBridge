\subsection{\href{https://beta.lmfdb.org/knowledge/show/nf.galois_group}{Galois group}}
\begin{definition}\label{nf.galois_group}
\uses{lf.automorphism_group,nf.degree,nf.galois_closure,nf.separable,ring.field}
Let $K$ be a finite \hyperref[nf.degree]{degree} $n$ \hyperref[nf.separable]{separable extension} of a \hyperref[ring.field]{field} $F$, and $K^{gal}$ be its
\hyperref[nf.galois_closure]{Galois (or normal) closure}. 
The \textbf{Galois group} for $K/F$ is the \hyperref[lf.automorphism_group]{automorphism group} $\Aut(K^{gal}/F)$.  

This automorphism group acts on the $n$ embeddings $K\hookrightarrow K^{gal}$ via composition.  As a result, we get an injection $\Aut(K^{gal}/F)\hookrightarrow S_n$, which is well-defined up to the labelling of the $n$ embeddings, which corresponds to being well-defined up to conjugation in $S_n$.

We use the notation $\Gal(K/F)$ for $\Aut(K/F)$ when $K=K^{gal}$.

There is a \hyperref[nf.galois_group.name]{naming convention} for Galois groups up to degree $47$.



\end{definition}


