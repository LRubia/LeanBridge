\subsection{\href{https://beta.lmfdb.org/knowledge/show/mf.maass.label}{Label of a Maass newform}}
\begin{definition}\label{mf.maass.label}
\uses{character.dirichlet,character.dirichlet.conrey,mf.maass.mwf,mf.maass.mwf.eigenvalue,mf.maass.mwf.level,mf.maass.mwf.weight,mf.maass.spectral_index}
The \textbf{label} of a \hyperref[mf.maass.mwf]{Maass newform} $f \in M_k(\Gamma_0(N), \chi)$ either has the format $N.k.a.m.d$ or $N.m$, where

\begin{itemize}
\item  $N$ is the \hyperref[mf.maass.mwf.level]{level};
\end{itemize}

\begin{itemize}
\item  $k$ is the \hyperref[mf.maass.mwf.weight]{weight};
\end{itemize}

\begin{itemize}
\item  $N.a$ is the \hyperref[character.dirichlet.conrey]{Conrey label} of the \hyperref[character.dirichlet]{Dirichlet character} $\chi$;
\end{itemize}

\begin{itemize}
\item  $m$ is the \hyperref[mf.maass.spectral_index]{spectral index}, which uniquely identifies the \hyperref[mf.maass.mwf.eigenvalue]{eigenvalue} $\lambda$ of $f$;
\end{itemize}

\begin{itemize}
\item  $d$ indicates which form among those of level $N$, Dirichlet character $N.a$, and eigenvalue $\lambda$ this is.
\end{itemize}

The shorter form is used when $k=0$ and $a=1$ and $d=1$.

For squarefree level, it is conjectured that there is only one Maass form per eigenvalue $\lambda$, hence $d$ is (currently) always $1$. Generally, there are only finitely many Maass newforms in $M_k(\Gamma_0(N), \chi)$ with the same eigenvalue $\lambda$. Then $d$ denotes the index among these finitely many forms, where the ordering is lexicographical in the sequence $\{ \bigl(\mathrm{Re}(a(n)), \mathrm{Im}(a(n))\bigr) \}_{n \geq 1}$.
\end{definition}


